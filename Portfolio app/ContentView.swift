// this isnt for nea its just so I can preview so dont add this file

import SwiftUI

struct ContentView: View {
    var body: some View {
        LoginPage()
    }
}


#Preview {
    ContentView()
}
